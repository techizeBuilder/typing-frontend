# ⚠️  DEPLOYMENT NOTE:
# This frontend URL MUST match the API_BASE_URL in:
#   1. /API_CONFIG.js (main config file)
#   2. /backend/.env (backend)
# Update all three when deploying!

VITE_API_BASE_URL=http://localhost:3012/api
