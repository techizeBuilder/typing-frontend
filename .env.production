# Production API base URL — baked into the desktop build (vite build runs in
# production mode). Pages that read import.meta.env.VITE_API_BASE_URL use this.
VITE_API_BASE_URL=https://balajityping.in/api
